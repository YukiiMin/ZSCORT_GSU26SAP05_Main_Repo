<mvc:View
  controllerName="zscort.app.controller.Compare"
  xmlns="sap.m"
  xmlns:mvc="sap.ui.core.mvc"
  xmlns:f="sap.f"
  xmlns:core="sap.ui.core"
  xmlns:table="sap.ui.table"
  xmlns:layout="sap.ui.layout"
  height="100%">

  <f:DynamicPage id="idDetailDynamicPage" toggleHeaderOnTitleClick="false" class="sapUiNoContentPadding">
    <f:title>
      <f:DynamicPageTitle>
        <f:heading>
          <Title text="{detail>/ObjectType} {detail>/ObjectName}" />
        </f:heading>
        <f:navigationActions>
          <Button icon="sap-icon://full-screen" type="Transparent" press=".onButtonFullScreenPress" tooltip="Toggle Full Screen" />
          <Button icon="sap-icon://decline" type="Transparent" press=".onButtonCloseComparePress" tooltip="Close" />
        </f:navigationActions>
      </f:DynamicPageTitle>
    </f:title>

    <f:content>
      <IconTabBar id="idCompareIconTabBar" class="sapUiNoContentPadding" stretchContentHeight="true" select=".onTabSelect">
        <items>
          <!-- TAB 1: Git Review (Local vs Target) -->
          <IconTabFilter key="gitReview" text="Git Review (Local vs Target)" icon="sap-icon://compare-2">
            <layout:Splitter orientation="Vertical" width="100%" height="100%">
              <VBox height="auto">
                <layoutData>
                  <layout:SplitterLayoutData size="auto" />
                </layoutData>
                <OverflowToolbar id="idGitReviewToolbar">
                  <ObjectStatus text="Local (Active)" state="Indication05" class="sapUiTinyMarginEnd" />
                  <Title text=" vs " />
                  <ObjectStatus text="Target (Snapshot)" state="Indication04" class="sapUiTinyMarginBegin" />
                  <ToolbarSpacer />
                  <Button icon="sap-icon://navigation-up-arrow" text="Prev Diff" type="Transparent" press=".onMonacoPrevDiffGit" tooltip="Previous Difference" />
                  <Button icon="sap-icon://navigation-down-arrow" text="Next Diff" type="Transparent" press=".onMonacoNextDiffGit" tooltip="Next Difference" />
                  <ToolbarSeparator />
                  <Button icon="sap-icon://header" text="Single-Column" type="Transparent" press=".onMonacoToggleSideBySideGit" tooltip="Toggle Inline / Side-by-side" />
                </OverflowToolbar>
              </VBox>
              <VBox id="monacoGitContainer" width="100%" height="100%" class="sapUiNoContentPadding">
                <layoutData>
                  <layout:SplitterLayoutData size="auto" />
                </layoutData>
                <!-- Monaco Git Diff Injected Here -->
              </VBox>
            </layout:Splitter>
          </IconTabFilter>

          <!-- TAB 2: Version Management (ADT Style) -->
          <IconTabFilter key="versionMgmt" text="Version History (ADT Style)" icon="sap-icon://history">
            <layout:Splitter orientation="Vertical" width="100%" height="100%">
              <!-- Top Pane: Version Table -->
              <VBox height="100%">
                <layoutData>
                  <layout:SplitterLayoutData size="40%" />
                </layoutData>
                <table:Table
                  id="idVersionsTable"
                  selectionMode="MultiToggle"
                  rowSelectionChange=".onVersionSelectionChange"
                  visibleRowCountMode="Auto"
                  busy="{detail>/busyVersions}"
                  rows="{path: 'detail>/versions'}">
                  <table:extension>
                    <OverflowToolbar>
                      <Title text="Select 2 Versions to Compare" />
                      <ToolbarSpacer />
                      <Button icon="sap-icon://refresh" text="Refresh" press=".onReloadVersions" type="Transparent" />
                      <Button id="btnCompare" icon="sap-icon://compare-2" text="Compare Selected" type="Emphasized" press=".onComparePress" enabled="false" />
                    </OverflowToolbar>
                  </table:extension>
                  <table:columns>
                    <table:Column width="4rem" hAlign="Center">
                      <Label text="Status" />
                      <table:template>
                        <core:Icon
                          src="{= ${detail>VersionNo} === '99998' ? 'sap-icon://sys-enter-2' : 'sap-icon://sys-help-2' }"
                          color="{= ${detail>VersionNo} === '99998' ? 'Positive' : 'Critical' }" />
                      </table:template>
                    </table:Column>
                    <table:Column width="6rem">
                      <Label text="Server" />
                      <table:template>
                        <ObjectStatus text="{= ${detail>ServerType} === 'L' ? 'Local' : 'Target' }" state="{= ${detail>ServerType} === 'L' ? 'Information' : 'Warning' }" />
                      </table:template>
                    </table:Column>
                    <table:Column width="6rem">
                      <Label text="Version" />
                      <table:template>
                        <Text text="{path: 'detail>VersionNo', formatter: '.formatVersionNo'}" />
                      </table:template>
                    </table:Column>
                    <table:Column width="8rem">
                      <Label text="Author" />
                      <table:template>
                        <Text text="{detail>Author}" />
                      </table:template>
                    </table:Column>
                    <table:Column width="10rem">
                      <Label text="Date / Time" />
                      <table:template>
                        <Text text="{path: 'detail>Datum', formatter: '.formatDate'} {detail>Uzeit}" />
                      </table:template>
                    </table:Column>
                    <table:Column width="10rem">
                      <Label text="Request" />
                      <table:template>
                        <Text text="{detail>Korrnum}" />
                      </table:template>
                    </table:Column>
                    <table:Column width="auto">
                      <Label text="Message / Request Text" />
                      <table:template>
                        <Text text="{detail>Message}" wrapping="false" />
                      </table:template>
                    </table:Column>
                  </table:columns>
                </table:Table>
              </VBox>

              <!-- Bottom Pane: Monaco Editor for Version History -->
              <VBox height="100%" visible="{detail>/showEditor}" fitContainer="true" class="sapUiNoContentPadding">
                <layoutData>
                  <layout:SplitterLayoutData size="60%" />
                </layoutData>
                <OverflowToolbar id="idEditorToolbar" visible="{detail>/showEditor}">
                  <ObjectStatus text="Left: {detail>/leftTitle}" state="Indication05" class="sapUiTinyMarginEnd" />
                  <Title text=" vs " />
                  <ObjectStatus text="Right: {detail>/rightTitle}" state="Indication04" class="sapUiTinyMarginBegin" />
                  <ToolbarSpacer />
                  <Button icon="sap-icon://navigation-up-arrow" text="Prev Diff" type="Transparent" press=".onMonacoPrevDiffVers" tooltip="Previous Difference" />
                  <Button icon="sap-icon://navigation-down-arrow" text="Next Diff" type="Transparent" press=".onMonacoNextDiffVers" tooltip="Next Difference" />
                  <ToolbarSeparator />
                  <Button icon="sap-icon://header" text="Single-Column" type="Transparent" press=".onMonacoToggleSideBySideVers" tooltip="Toggle Inline / Side-by-side" />
                </OverflowToolbar>

                <VBox id="monacoVersContainer" width="100%" height="100%" visible="{detail>/showEditor}">
                  <layoutData>
                    <FlexItemData growFactor="1" />
                  </layoutData>
                  <!-- Monaco Version Diff Injected Here -->
                </VBox>
              </VBox>
            </layout:Splitter>
          </IconTabFilter>
        </items>
      </IconTabBar>
    </f:content>
  </f:DynamicPage>

</mvc:View>
